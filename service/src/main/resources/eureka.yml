eureka:
  instance:
    instance-id: ${spring.application.name}:${random.uuid}
    app-group-name: justedlev-hub
    appname: ${spring.application.name}
  client:
    service-url:
      default-zone: ${application.service-registry.url}
